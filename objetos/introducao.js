// Linguagem Procedural
processamento(valor1, valor2, valor3) // Funções possuem dados

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        // ...
    }
}

objeto.processamento()  // Dados possuem funções
                        // Foco passou a ser o objeto

/*
    Princípios importantes:
    1. Abstração -> Trazer objetos do mundo real para o sistema
    2. Encapsulamento -> Detalhes de implementação escondidos, mostrar apenas o necessário
    3. Herança(prototype) -> Recebe atributos e comportamentos de um objeto pai
    4. Polimorfismo -> Múltiplas formas
 */